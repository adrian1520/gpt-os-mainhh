name: AGENT TYPE
on:
  repository_dispatch:
    types: [trigger_type]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run parser_type
        run: |
          python modules/agents/parser_type.py

      - name: SAFE PUSH
        run: |
          git config user.name "gpt-os"
          git config user.email "gpt@os.local"
          for i in {1..5}; do
            git pull --rebase origin main && break
            sleep $((RANDOM % 5))
          done
          git add events/*
          git commit -m "type event" || echo "no changes"
          git push
