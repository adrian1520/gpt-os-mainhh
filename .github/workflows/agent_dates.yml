name: AGENT DATES
on:
  repository_dispatch:
    types: [trigger_dates]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run agent_dates
        run: |
          python modules/agents/parser_dates.py

      - name: SAFE PUSH
        run: |
          git config user.name "gpt-os"
          git config user.email "gpt@os.local"
          for i in {1..5}; do
            git pull --rebase origin main && break
            sleep $((RANDOM % 5))
          done
          git add events/*
          git commit -m "dates event" || echo "no changes"
          git push
